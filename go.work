go 1.26

use (
	.
	./cmd/gs
)

// 本地开发: cmd/gs 的 require 写的是已发布版本号 v0.3.0; 该 tag 已打到
// 远程后即可解析, 但开发期用版本精确 replace 指向同仓库父目录, 避免依赖
// 模块代理的同步时延。go.work 不参与 go install (install 只认已发布 tag)。
replace github.com/ejfkdev/gs v0.3.0 => .